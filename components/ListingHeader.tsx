import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ListingHeader = () => {
  return (
    <View>
      <Text>ListingHeader</Text>
    </View>
  );
};

export default ListingHeader;

const styles = StyleSheet.create({});
